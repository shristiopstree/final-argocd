apiVersion: v1
kind: Service
metadata:
  name: {{ include "argo-bluegreen.fullname" . }}-active
  namespace: {{ .Values.global.namespace | quote }}
  labels:
    {{ include "argo-bluegreen.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{ include "argo-bluegreen.selectorLabels" . | nindent 4 }}
  ports:
    {{- range .Values.service.specs }}
    - name: {{ .name }}
      port: {{ .port }}
      protocol: {{ .protocol | default "TCP" }}
      targetPort: {{ .targetPort | default .port }}
    {{- end }}

---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "argo-bluegreen.fullname" . }}-preview
  namespace: {{ .Values.global.namespace | quote }}
  labels:
    {{ include "argo-bluegreen.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{ include "argo-bluegreen.selectorLabels" . | nindent 4 }}
  ports:
    {{- range .Values.service.specs }}
    - name: {{ .name }}
      port: {{ .port }}
      protocol: {{ .protocol | default "TCP" }}
      targetPort: {{ .targetPort | default .port }}
    {{- end }}
